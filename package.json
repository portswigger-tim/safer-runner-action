{
  "name": "safer-runner",
  "version": "1.0.0",
  "description": "Network security layer for GitHub Actions with DNS filtering and log analysis",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && ncc build lib/main.js -o dist/main --license licenses.txt && mv dist/main/index.js dist/main.js",
    "build:post": "ncc build lib/post.js -o dist/post --license licenses.txt && mv dist/post/index.js dist/post.js",
    "clean": "rm -rf dist lib",
    "package": "npm run clean && npm run build && npm run build:post",
    "test": "jest",
    "all": "npm run package"
  },
  "keywords": ["security", "github-actions", "dns", "firewall"],
  "author": "PortSwigger",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.2.2"
  }
}
